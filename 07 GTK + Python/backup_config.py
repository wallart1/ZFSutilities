"""Backup configuration compatibility shim.

Re-exports the public API from logging_config, config_core, and feature_config
so existing callers can be migrated incrementally.
"""

from config_core import (  # noqa: F401
    BACKUP_DEFAULTS,
    CONFIG_PATH,
    CONFIG_VERSION,
    DASHBOARD_DEFAULTS,
    DEFAULT_HISTORY_RETENTION_DAYS,
    DEFAULT_LOG_RETENTION_DAYS,
    SESSION_LOG_DIR,
    UI_STATE_DEFAULTS,
    _deep_copy,
    get_dashboard_config,
    get_docs_editor,
    get_history_retention_days,
    get_log_retention_days,
    get_profiles_dir,
    get_ui_state,
    load_config,
    prune_old_logs,
    save_config,
    save_dashboard_config,
    save_docs_editor,
    save_history_retention_days,
    save_log_retention_days,
    save_msg_level,
    save_ui_state,
)
from feature_config import (  # noqa: F401
    DEFAULT_RETENTION,
    OFFSITE_DEFAULTS,
    OFFSITE_SNAPFILE,
    RESTORE_DEFAULTS,
    SCRUB_MANAGER_DEFAULTS,
    SCRUB_STATE_PATH,
    SNAPFILE,
    SNAPNAME_LOCK,
    SNAPNAME_RESERVE_SECONDS,
    SNAPNAME_RESERVED,
    _build_snapshot_name,
    _ensure_snapname_dirs,
    _is_snapshot_name_reserved,
    _load_reservations,
    _read_snapfile,
    _remove_snapfile,
    _reserve_snapshot_name,
    _reserve_snapshot_name_unlocked,
    _save_reservations,
    _snapname_lock,
    _write_snapfile,
    generate_offsite_snapshot_name,
    generate_snapshot_name,
    get_all_retention,
    get_archive_path,
    get_backup_config,
    get_checkagainst,
    get_offsite_candidate_names,
    get_offsite_config,
    get_pool_names,
    get_pools,
    get_prune_label,
    get_prune_pools_order,
    get_restore_config,
    get_retention,
    get_scrub_manager_config,
    import_legacy_retention,
    load_scrub_state,
    remove_snapfile,
    save_archive_path,
    save_backup_config,
    save_checkagainst,
    save_offsite_config,
    save_offsite_snapshot_name,
    save_pools,
    save_prune_label,
    save_prune_pools_order,
    save_restore_config,
    save_retention,
    save_scrub_manager_config,
    save_scrub_state,
    save_snapshot_name,
)
from logging_config import (  # noqa: F401
    DEFAULT_MSG_LEVEL,
    MSG_LEVELS,
    get_log_sink,
    get_msg_level,
    log_msg,
    set_log_sink,
)
