# Disks Page — Working Progress Notes

## References

- Design brief: `/NFS1/dan(NFS1)/zfsutilities-plan/Disks page/README.md`
- Per-phase briefs: `/NFS1/dan(NFS1)/zfsutilities-plan/Disks page/`

## Completed

- [x] Phase 0: created git branch `Disks-Page` (git rejected the requested name `Disks Page` because spaces are invalid in branch names), added `AGENTS.md` reference, created this progress file.
- [x] Phase 1 — Inventory + topology + capability layer (read-only). Implemented in branch `Disks-Page`.
- [x] Refresh Disks tab on every page entry (including first entry after GUI restart) via `on_page_changed` hook.
- [x] Phase 2 — Workload profiles + dataset tuning. Implemented in branch `Disks-Page`.
- [x] Phase 3 — Create-pool wizard. Implemented in branch `Disks-Page`.
- [x] Phase 4 step 5 — Replace operation: `classify_replace_source` in `pool_growth.py`; `show_replace_dialog` + `on_disks_replace_device` in `pool_growth_dialogs.py` (typed confirmation always required, smaller-replacement warning, scrub gate, pool-scope lock, one fatal `BashStep`, resilver Watch-window pointer); tests in `test_pool_growth.py` / `test_pool_growth_dialogs.py`. Note: `_build_target_tree`/`_populate_target_store` gained a `gate_raidz` flag (default `True`) so the Replace dialog does not grey raidz rows with the RAIDZ-expansion `requires()` text.
- [x] Phase 4 step 6 — Detach operation: `_DetachState`, `build_detach_argv`, `_detach_problems`, `_detach_warnings`, and `show_detach_dialog` in `pool_growth_dialogs.py` (mirror-leaf-only via the existing `pool_growth.assess_detach`, typed confirmation always required — detach is irreversible and reduces redundancy, scrub gate, no disk picker/eligibility scan since detach consumes no new disk); `on_disks_detach_device` handler (pool-scope lock, one fatal `BashStep`, refresh); `_build_target_tree` gained an optional `hint_text` parameter; tests in `test_pool_growth_dialogs.py`. Also fixed the step-1 `build_detach_command` docstring to reference `assess_detach` (was `validate_detach_target`).
- [x] Phase 4 step 7 — Add Infrastructure Vdev operation: `_InfraVdevState`, `_infra_topology` (auto-derives mirror ≥2 disks / stripe 1), `build_infra_argv`, `_infra_problems`, `_infra_warnings`, `_infra_checkbox_label`, and `show_add_infra_vdev_dialog` in `pool_growth_dialogs.py` (kind selector for special/log/cache over the shared disk picker; per-kind confirmation — typed pool-name for special, acknowledgment checkbox for log, YES/NO question for cache; scrub gate; reuses the step-1 `build_add_vdev_command(kind=…)` and step-2 `validate_infra_vdev`/`infra_vdev_notes`); `_ReviewScaffold` gained a hidden `confirm_checkbox` with a `checkbox_label` refresh parameter; `_show_yes_no_dialog` helper; `on_disks_add_infra_vdev` handler (pool-scope lock, one fatal `BashStep`, refresh); tests in `test_pool_growth_dialogs.py`.
- [x] Phase 4 step 8 — Dispatch wiring + sensitivity: five Disks-page buttons in `action_dispatch.py` (`Add Data Vdev…`, `Attach…`, `Replace…`, `Detach…`, `Add Infra Vdev…` between Create Pool… and Apply Profile…, attrs `_disks_{add_vdev,attach,replace,detach,add_infra_vdev}_btn`) mapped to the `pool_growth_dialogs` handlers; `update_disks_button_sensitivity` in `disks_page.py` gates all five with the Create Pool pattern (two-node compute-host disable taking precedence, then runner-busy disable; growth vs. maintenance storage-host tooltips); tests in `test_action_dispatch.py` and new `test_disks_page_phase4.py`.
- [x] Phase 4 step 9 — Docs + final verification: new "Growing and Maintaining Pools" section (per-op subsections with warnings and confirmation styles) in `docs/docs/user-guide/gtk-gui.md` plus Actions-list entries and feature→min-version rows (RAIDZ expansion 2.3+); `pool_growth.py` / `pool_growth_dialogs.py` entries in `docs/docs/commands-and-modules/python-modules.md` and the architecture module table; `AGENTS.md` Phase 4 paragraph. `ruff check .` clean; full Python suite (71 suites) and full bash suite green; `test_docs_integrity` passes.

- [x] Phase 2 follow-up — Dataset Tuning Size column (`COL_DS_USED`, populated from `DatasetRow.used`, no extra `zfs` call) and Apply Profile picker treeview (Name / Applies to / wrapped Description columns, single selection, pre-selects the first selected dataset's profile match, mock-safe selection reads); tests updated/added in `test_disks_page_phase2.py` (58 tests); docs in `docs/docs/user-guide/gtk-gui.md` and `docs/docs/developer-guide/testing.md`.
- [x] Topology-selection highlighting — selecting a pool, vdev, or device node in the Pool Topology pane now tints the corresponding member rows in the Disk Inventory via the existing `COL_D_HIGHLIGHT` mechanism (`_topology_subtree_device_paths`, `_path_matches_any_device`, `_highlight_topology_devices` in `disks_page.py`); clearing the selection restores the pool-wide highlight; partition leaves also tint the whole-disk row; tests in `test_disks_page.py` (28 tests); docs updated.

## Next

- Phase 4 complete — all 9 coding-plan steps done, uncommitted on `Disks-Page`. Commit only on the user's explicit instruction.
